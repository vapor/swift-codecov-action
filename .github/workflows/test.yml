name: test
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
on:
  push: { branches: [ 'main' ] }
  workflow_dispatch:

jobs:
  linux-containerized-toplevel:
    strategy:
      fail-fast: false
      matrix:
        swift: 
          - swift:6.3-noble
          - swiftlang/swift:nightly-main-noble
    runs-on: ubuntu-latest
    container: ${{ matrix.swift }}
    steps:
      - run: apt-get update -y && apt-get install -y curl
      - uses: actions/checkout@v6
      - run: swift test --enable-code-coverage
      - uses: vapor/swift-codecov-action@main
        with:
          codecov_token: ${{ secrets.CODECOV_TOKEN }}
          dry_run: true
          verbose: true

  linux-containerized-subpath:
    strategy:
      fail-fast: false
      matrix:
        swift:
          - swift:6.3-noble
          - swiftlang/swift:nightly-main-noble
    runs-on: ubuntu-latest
    container: ${{ matrix.swift }}
    steps:
      - run: apt-get update -y && apt-get install -y curl
      - uses: actions/checkout@v6
      - run: swift test --package-path sample-coverage-data --enable-code-coverage
      - uses: vapor/swift-codecov-action@main
        with:
          codecov_token: ${{ secrets.CODECOV_TOKEN }}
          dry_run: true
          verbose: true
          package_path: sample-coverage-data

  macos-toplevel:
    runs-on: macos-26
    steps:
      - uses: maxim-lobanov/setup-xcode@ed7a3b1fda3918c0306d1b724322adc0b8cc0a90 # v1.7.0
        with: { xcode-version: latest-stable }
      - uses: actions/checkout@v6
      - run: swift test --enable-code-coverage
      - uses: vapor/swift-codecov-action@main
        with:
          codecov_token: ${{ secrets.CODECOV_TOKEN }}
          dry_run: true
          verbose: true

  macos-subpath:
    runs-on: macos-26
    steps:
      - uses: maxim-lobanov/setup-xcode@ed7a3b1fda3918c0306d1b724322adc0b8cc0a90 # v1.7.0
        with: { xcode-version: latest-stable }
      - uses: actions/checkout@v6
      - run: swift test --package-path sample-coverage-data --enable-code-coverage
      - uses: vapor/swift-codecov-action@main
        with:
          codecov_token: ${{ secrets.CODECOV_TOKEN }}
          dry_run: true
          verbose: true
          package_path: sample-coverage-data

  windows:
    runs-on: windows-latest
    steps:
      - name: Install Windows Swift toolchain
        uses: compnerd/gha-setup-swift@eeda069c5bc95ac8a9ac5cea7d4f588ae5420ca5 # v0.4.0
        with:
          swift-version: swift-6.3-release
          swift-build: swift-6.3-RELEASE
          update-sdk-modules: true
      - uses: actions/checkout@v6
      - run: |
          swift test --package-path sample-coverage-data --enable-code-coverage
      - uses: vapor/swift-codecov-action@main
        with:
          codecov_token: ${{ secrets.CODECOV_TOKEN }}
          dry_run: true
          verbose: true
          package_path: sample-coverage-data
